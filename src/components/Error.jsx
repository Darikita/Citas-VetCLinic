const Error = ({children}) => {
    return (
        <div className="bg-melon text-white text-center p-3 uppercase font-bold mb-3 rounded-md">
            {children}
        </div>
    )
}

export default Error
